use itertools::Itertools;
use itertools_num::ItertoolsNum;
use proconio::input;

fn main() {
  input! {
    n: usize,
    q: usize,
    x: usize,
    ws: [usize; n],
    ks: [usize; q],
  }

  // println!("{}", ans);
}